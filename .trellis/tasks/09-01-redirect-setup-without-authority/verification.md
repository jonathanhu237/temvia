# Redirect setup without authority verification

Verified 2026-09-01 from the source template, packed/generated output, and the
forwarded Centaurus runtime.

## Static and generated checks

- Admin Oxlint, TypeScript check, 39 Vitest tests, and production build passed.
- Root type-check/build, 12 CLI tests, 5 Git tests, and 2 package tests passed.
- The new ignored route test is present in the final admin inventory and the
  generator preflight; packed/generated byte parity passed.
- The generated Centaurus admin passed lint, type-check, 39 tests, production
  build, and the complete Chromium authentication flow.
- `git diff --check` and Trellis context validation passed.

## Routing and runtime checks

- Loader unit tests prove that missing authority redirects before calling
  `getSetupStatus`, captured authority allows the status query, and completed
  setup clears authority before redirecting.
- Chromium verified bare `/setup` redirects to `/login` with zero
  `/api/setup/status` requests.
- Chromium verified a valid setup link renders the administrator form and
  makes one setup-status request.
- Reloading the cleaned `/setup` URL redirects to `/login` and adds zero
  setup-status requests.
- Chromium verified a server-rejected setup token clears authority and returns
  to `/login`.
- The complete setup, login, session restoration, authenticated `/login`
  redirect, locale, responsive Sidebar and logout flow passed.
- Setup and login axe scans reported zero violations.
- Final manual evidence is saved at
  `evidence/setup-refresh-redirect.png`.

The disposable preview database was reset after testing. The final setup
status is `required`; the current token has only been opened, not submitted,
and remains available for manual initialization.
