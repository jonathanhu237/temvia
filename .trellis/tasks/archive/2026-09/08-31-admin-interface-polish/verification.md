# Generated admin interface polish verification

Verified on 2026-09-01 from the real packed CLI and a disposable generated
project. No source template was copied directly into the runtime project.

## Local source and package checks

- Trellis context manifests: valid (`implement.jsonl` 5 entries,
  `check.jsonl` 6 entries).
- Admin lint: passed with zero warnings/errors.
- Admin TypeScript: passed.
- Admin Vitest: 8 files, 26 tests passed.
- Admin production build: passed.
- Root `pnpm check`, `pnpm build`, and CLI test suite: passed (12 tests).
- `pnpm test:package`: passed (2 tests). It packed the actual npm tarball,
  installed it offline without development dependencies, generated away from
  the source tree, and verified source-to-tarball-to-generated bytes.
- `git diff --check`: passed.

The retained generated project was created from that packed artifact at
`/tmp/temvia-admin-polish-local.x1pz3c91/generated` and synchronized one way to
Centaurus at `/tmp/temvia-admin-polish-verified.k7q4mn/generated`.

## Centaurus generated-project checks

The generated admin ran under Node 24.20.0 and pnpm 11.24.0:

- `pnpm lint`: passed, zero warnings/errors.
- `pnpm check`: passed.
- `pnpm test`: passed, 8 files and 26 tests.
- `pnpm build`: passed; Vite transformed 2,188 modules.
- API, admin, and migrate container images built successfully.
- A fresh PostgreSQL 18 / Redis 8 stack passed explicit migration and started
  the API plus Caddy admin service.
- The Caddy admin image runs as `caddy` and contains neither Node at
  `/usr/local/bin/node` nor the build workspace at `/app`.
- `/api/setup/status` passes through Caddy and `/setup` receives the SPA shell.

## Browser and accessibility flow

Playwright Chromium against the generated Caddy origin passed:

- setup fragment removal and no browser-storage/cookie credential leak;
- concise setup Card assertions and one visible heading;
- administrator creation, neutral login Card, login, session reload, locale
  switch, desktop Sidebar geometry, mobile Sidebar navigation, logout, and axe;
- unauthenticated redirect to `/login` with no unexpected console errors.

The first invocation omitted `PLAYWRIGHT_BASE_URL` for the standalone redirect
test. Its main serial setup/authentication flow passed, while the later
relative `page.goto('/')` opened an unrelated service on Centaurus port 5173.
The redirect test passed after setting the verified origin to
`http://127.0.0.1:41173`. This produced the new explicit-base and mobile-context
rules in the frontend quality spec.

## Visual evidence

- `evidence/setup-desktop.png` — English setup Card, desktop.
- `evidence/setup-mobile.png` — English setup Card with compact accessible
  language trigger, 390 by 844 viewport.
- `evidence/setup-chinese-desktop.png` — Simplified Chinese setup Card.
- `evidence/login-desktop.png` — neutral title-only login Card.
- `evidence/home-expanded.png` — expanded Sidebar without a brand header.
- `evidence/home-collapsed.png` — collapsed Sidebar without an empty brand
  placeholder.
- `evidence/home-mobile.png` — fully settled mobile Sheet without branding or
  clipped content.

All screenshots were reviewed at original resolution. The repeated Home labels
remain unchanged by explicit product decision. The first mobile evidence
attempt resized a mounted desktop context and captured before the Sheet state
settled; it was replaced by a fresh mobile context that waits for the visible
mobile Sidebar and its transition.

## Live review stack

The final preview stack was reset to a fresh, uninitialized database after the
browser flow. It remains running on Centaurus as Compose project
`temvia_admin_polish`, with the admin forwarded locally on port 41173. The
preview setup-link lifetime was extended only in the private runtime `.env` to
two hours; the generated `.env.example` default remains 30 minutes.
