# 登录校验提示

Status: resolved

## 已确认范围

用户确认：初始及首次失焦不显示错误，提交后才校验；空密码提示“请输入密码”，超长密码提示“密码不能超过 128 个字符”。字段错误仅在字段下显示，不叠加顶部通用错误。凭据不匹配统一提示“邮箱或密码错误”。保留原有登录密码兼容性和服务端限制，不应用新密码强度策略。

## 一并验收的用户菜单调整

用户行操作收纳到“⋯”菜单，保留分配角色、停用/恢复和删除确认。表头与按钮统一居中；真实浏览器检查中心线偏差小于 1px。菜单打开、确认框取消及重新打开验证通过。

## 实施与验证

已实现中英文区分提示、提交校验、服务端字段错误去重和新提交清理过时顶部错误。
前端类型检查、134 项单元/组件测试和生产构建通过。真实浏览器中文深色登录页验证首次失焦无错误、空提交单条必填提示、129 字符单条长度提示。
用户已 LGTM，并授权提交。
