# Verification

The earlier sections below record implementation-agent test runs, not a claim that no later operational incident occurred. A subsequent erroneous remote rsync deleted temporary files; it was disclosed and documented separately. The user authorized resuming without recovery. The final parent-run checks at the end of this document supersede earlier completion claims. No further rsync used `--delete`.

- Local `node --check scripts/first-run-acceptance.mjs`, `pnpm check`, `pnpm build`, `pnpm test`, `pnpm test:git`, `pnpm test:release`, and `pnpm test:package`: passed.
- Local generated-admin `pnpm lint`, `pnpm check`, and `pnpm test`: passed (135 tests); lint retained one pre-existing TanStack `react(incompatible-library)` warning and no errors.
- Centaurus (`mise exec`): generator checks/build/tests, Git tests, package tests, API `go test ./...`, `go vet ./...`, `go build`, and generated-admin lint/check/tests/build passed. Admin lint retained one pre-existing TanStack `react(incompatible-library)` warning and no errors.
- Centaurus release-tarball gate: `pnpm test:package` produced the tested tarball, then `node scripts/first-run-acceptance.mjs <that-tarball>` passed. It generated outside the source tree, built isolated Compose images, verified the migration image inventory, migrated an empty PostgreSQL volume, extracted the fresh setup link from API logs, created the administrator in Chromium, explicitly logged in, reloaded the protected page, and cleaned the temporary Compose containers/volume.
- Centaurus upgrade run: an isolated generated project first ran the baseline images and stored a sentinel row; after adding a temporary numbered migration and API build marker, the documented `docker compose stop api` -> `pg_dump` backup -> `make build` -> `make migrate-up` -> `docker compose up -d api admin` sequence passed. The backup contained the sentinel, the new migration table was present, the sentinel remained, and API/migration image IDs changed. The isolated project, network, and volume were removed afterward.
- `git diff --check`: passed. No commit, push, publication, or existing Centaurus project resources were touched.

## Review-1 fix verification

All commands below ran in the unique Centaurus workspace `/tmp/temvia-template-readiness-fix2-20260912T184127Z-65473` through `mise exec`; the local tree was synchronized one way with `rsync` before each remote run.

- R1 unit regression: `mise exec -- node --test tests/first-run-acceptance.test.mjs` (also included by `mise exec -- pnpm test`) passed. The adversarial fixture covered inherited `POSTGRES_*`, `PG*`, `COMPOSE_*`, `APP_PUBLIC_URL`, `E2E_*`, `PLAYWRIGHT_*`, and npm installation-routing variables while retaining `PATH`, HTTPS proxy, and `GOPROXY` support.
- Generator/package regression: `mise exec -- pnpm check && mise exec -- pnpm build && mise exec -- pnpm test && mise exec -- pnpm test:git && mise exec -- pnpm test:release && mise exec -- pnpm test:package` passed. The package test verified both generated language guides use a private `mktemp` backup directory with `umask 077`, modes 0700/0600, and that generated `.gitignore` excludes `backups/`.
- R3 settings component: `cd template/admin && mise exec -- pnpm exec vitest run src/features/settings/settings.test.tsx` passed (11 tests), including the user-visible identity-bound password error and successful retry with a replacement password. Full generated-admin `mise exec -- pnpm test` passed (136 tests); `check`, `lint`, and `build` passed, with the existing TanStack `react(incompatible-library)` warning only.
- R3 SMTP application behavior: `TestSettingsTestSendsWithReplacementAndSameIdentityPassword` passed with an explicitly supplied replacement and an unchanged-identity omitted password; the fake mailer recorded successful sends and the store save count remained unchanged for test sends.
- Real PostgreSQL integration: an ephemeral `postgres:18.6-trixie` container was started on a dynamically allocated loopback port, every `template/api/migrations/*.up.sql` was applied, and `schema_migrations` was initialized at version 10/clean. With an explicit ephemeral `TEST_POSTGRES_DSN` (password omitted from this record), `cd template/api && mise exec -- env TEST_POSTGRES_DSN="$DSN" go test -count=1 -p 1 ./...` passed for all API packages. `mise exec -- go vet ./... && mise exec -- go build ./...` also passed.
- Final tarball/isolation gate: `mise exec -- npm pack --json --ignore-scripts --pack-destination .release`, followed by `POSTGRES_HOST=existing-db.invalid POSTGRES_DB=existing POSTGRES_PASSWORD=redacted PGHOST=existing-db.invalid COMPOSE_FILE=/existing/compose.yaml COMPOSE_PROJECT_NAME=existing-project APP_PUBLIC_URL=https://existing.invalid API_PORT=443 ADMIN_PORT=443 E2E_SETUP_URL=https://existing.invalid/setup#token=redacted PLAYWRIGHT_BASE_URL=https://existing.invalid NPM_CONFIG_PREFIX=/existing npm_config_registry=https://existing.invalid mise exec -- node scripts/first-run-acceptance.mjs .release/create-temvia-0.2.0.tgz`, passed. It used the exact freshly packed tarball, verified all migrations, built isolated Compose images, migrated an empty volume, completed browser setup/login/reload, and cleaned its containers, volume, network, and images. A post-run check found no `temvia-readiness` acceptance or PostgreSQL containers/networks.
- Post-fix rerun in the fresh Centaurus workspace `/tmp/temvia-template-readiness-fix2-final-20260912T185040Z-53928`: `mise exec -- node --check scripts/first-run-acceptance.mjs && mise exec -- node --test tests/first-run-acceptance.test.mjs && mise exec -- pnpm check && mise exec -- pnpm build`, the focused settings test, `mise exec -- pnpm test`, `test:git`, `test:release`, `test:package`, and the full generated-admin `test`, `check`, `lint`, and `build` all passed. The post-fix adversarial-environment tarball gate and an uncached full Go suite against an isolated PostgreSQL with explicit `TEST_POSTGRES_DSN` also passed. The verification workspace and its temporary containers were removed at that point. A later erroneous rsync did affect unrelated temporary resources; the earlier cleanup observation must not be interpreted as an overall no-impact claim.

## Final parent verification after direct R2 correction

Fixed review baseline: `ae41e85cfde580e5bffea74937680590a3d875aa`; fixed spec unchanged. Work ran in the explicit dedicated Centaurus directory `/home/jonathanhu237/work/temvia-parent-verify-20260912-1920`. Local source was copied with `rsync -az` to that literal destination, without deletion flags or externally stored path variables.

- Generator: `mise exec -- pnpm install --frozen-lockfile --ignore-scripts`, `pnpm check`, `pnpm build`, `pnpm test`, `pnpm test:git`, `pnpm test:release`, and `TEMVIA_TEST_TARBALL=<workspace>/create-temvia-verified.tgz pnpm test:package` all passed. Results: 24 generator/acceptance/upgrade-guide cases (including subtests), 5 Git cases, 10 release cases, 2 package cases; zero failures/skips. Evidence: local `parent-generator.log`.
- Direct R2 fix: both upgrade guides use a private, persistent HOME-based directory, not TMPDIR, and a fail-fast subshell. Successful dumps are renamed from `.partial` only after pg_dump succeeds. Behavioral tests execute both language snippets with fake external docker/make commands, covering success and failed stop, backup, build, or migration; they assert no later deployment step runs, directory/file modes 0700/0600, and no backup in the project tree. Included in the 24 cases above.
- Admin: `cd template/admin && pnpm install --no-frozen-lockfile --ignore-scripts && pnpm lint && pnpm check && pnpm test && pnpm build` passed on Centaurus. 21 test files / 136 tests passed, including settings error-and-retry behavior. One pre-existing lint warning, zero errors. Evidence: local `parent-admin.log`.
- Exact-tarball gate: `mise exec -- env POSTGRES_HOST=do-not-use.invalid POSTGRES_DB=existing POSTGRES_PASSWORD=do-not-use COMPOSE_FILE=/does-not-exist/compose.yaml COMPOSE_PROJECT_NAME=do-not-use APP_PUBLIC_URL=https://do-not-use.invalid PGHOST=do-not-use.invalid E2E_SETUP_URL=https://do-not-use.invalid/setup PLAYWRIGHT_BASE_URL=https://do-not-use.invalid node scripts/first-run-acceptance.mjs <workspace>/create-temvia-verified.tgz` passed: fresh generated images, inventory, migrations, setup, explicit browser login and reload. Evidence: local `parent-first-run.log`.
- Negative release gate: made a separate copy of that tarball whose migration Dockerfile deliberately omitted v10, then invoked the same acceptance entry point. It exited nonzero with `migration image inventory differs`; the parent fixture asserts this failure. No production/source template was changed for this check. Evidence: local `parent-negative-gate.log`.
- PostgreSQL: generated a separate unique Compose project `temvia-parent-upgrade-20260912-1920`, built and ran its actual migration image to version 10. After stopping only that fixture's API, derived the ephemeral DSN from its private .env without printing it and ran `cd template/api && TEST_POSTGRES_DSN=<isolated fixture DSN> go test -count=1 -p 1 ./...` through mise, then `go vet ./...` and `go build ./...`. All 8 packages passed, including HTTP/PostgreSQL integration suites; no manual schema-version bypass. Evidence: local `parent-postgres.log`.
- Real upgrade: seeded a harmless sentinel in the isolated fixture database, applied locally prepared harmless build labels to its generated API/admin/migration Dockerfiles, and executed the English README upgrade block verbatim in a shell with a dedicated test HOME. All three image IDs changed, backup mode was 0600 inside a 0700 persistent directory, dump contained the sentinel, and the database retained the sentinel at clean schema version 10 after restart. This is an image-rebuild/data-retention test, not a new schema-upgrade test. Evidence: local `parent-upgrade.log`; baseline template migrations independently validated above.
- Forwarded fixture API/admin/PostgreSQL ports 46121/46122/46123 to local loopback using a dedicated SSH control socket. Local requests verified API health, setup-status proxy and admin HTML; after upgrade API health and admin HTTP 200 remained available. Evidence: local `parent-forwarded-services.log`.
- Removed only the named fixture's containers, volume, network and tagged images, and closed its dedicated SSH forwarding connection. Kept the dedicated verification source/backup directory for evidence; no blanket remote directory deletion. Evidence: local `parent-cleanup.log` and `parent-artifact.log`.
- `git diff --check` passed. No push or public publication; actual GitHub-hosted workflow execution remains to be observed after a separately authorized push. No claim of comprehensive production/security/cross-platform certification.
