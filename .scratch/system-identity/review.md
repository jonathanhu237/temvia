# 系统身份实现审查

Baseline: `477e8603b2bebef657947046f5806a6fc3ac2caa`
Scope: 本地工作区改动及新增文件；不创建提交。

## 第 1 轮

Status: resolved

### Standards

- S1：移除系统身份分区的通用说明性副标题，遵守 AGENTS.md 的后台页面文案约定；保留字段规则。

### Spec

- F1：补齐真实浏览器的中英文及回退、公开页面、刷新持久化、保存默认、只读权限、并发冲突与重新加载、操作历史读取验收。
- F2：补齐邀请、重置、密码修改及测试邮件的自定义双语名称、英文缺省回退、HTML 转义、收件人及链接不受影响、独立发件人名称和依赖故障重试测试。
- F3：补齐 HTTP 名称去空白、必填及长度边界、JPEG/WebP 成功、非法格式及大小、写入拒绝及只读读取测试；验证失败不会改变已发布配置。
- F4：浏览器用例应在修改前保存原图内容。保存后再读取旧版本 URL 会读到当前图片，无法正确恢复原自定义图标。

### 验证范围

- 已有远端记录：根项目 check、12 项 CLI、2 项打包；前端 119 项测试及构建；Go 全量测试；1 项图标浏览器用例。
- 独立视觉检查：1440px、390px 和折叠侧栏；一个主标题，移动端无横向溢出。截图在 `artifacts/`。
- 实现代理最后一处本地清理尚未同步验证，须在修正后核验最终文件。

## 第 2 轮

Status: passed

### Standards

S1 已解决。移除了通用说明性副标题，保留字段规则和单一页面主标题。未发现新的阻断项。

### Spec

F1–F4 已解决。补齐双语公开页面、回退、刷新、冲突重载、默认图标保存、操作历史读取和自定义图标恢复验收；只读 UI 使用组件测试，并结合 HTTP 权限测试验证服务端拒绝写入。邮件测试覆盖自定义名称、转义、回退、独立发件人及依赖重试，HTTP 测试覆盖名称和图片边界。

最终验证在 Centaurus 完成：

- API：`go test ./...` 通过。
- Admin：120 项测试、check、build、lint 通过。
- 根项目：check、12 项 CLI 测试、2 项打包测试通过。
- 系统身份 Playwright 验收通过；自定义图标恢复后的采样像素、尺寸和透明边缘与恢复前一致。
- `rsync -rcni` 无代码差异，关键文件 SHA256 一致。中英文桌面与移动端截图已取回并目视检查。

无剩余阻断项。改动保留在工作区，未创建提交。

### 运行版本复核

交付前发现开发容器仍在提供旧前端资源，已从最终同步代码重建并重启 API/Admin。系统身份 Playwright 在更新后的容器上再次通过；额外确认设置页仅一个主标题、无通用说明性副标题，API 健康检查通过。最终设置截图为 `artifacts/settings-final.png`。

### 验收后的默认图标调整

用户要求采用 React 图标库，默认标识改为 Lucide Layers3；上传图片仍优先显示。设置预览与 favicon 同步使用叠层图形，并更新默认 favicon URL 避免旧图缓存。远端类型检查、6 项相关前端测试、应用及 HTTP 测试、容器构建、系统身份 Playwright 均通过。新版截图为 `artifacts/lucide-default.png`。用户 LGTM 后授权提交及推送。
