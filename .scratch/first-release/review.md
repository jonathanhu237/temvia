# 首版发布实现审查

Baseline: 5f0e6351ae23f272196ef4b684506b6305118286
Scope: 当前分支工作区与新增文件；按 implement-loop 由主代理直接审查，不启动审查子代理，不提交或发布。

## 第 1 轮

### Standards

0 findings。未发现文档规范或代码异味方面的阻断项。

### Spec

- R1 / P2：真实 macOS 浏览器流程在退出登录错误恢复处失败。现有用例仍断言旧错误横幅和专用 Retry 按钮，与已完成的统一 Sonner 反馈规格不符。应验证可见错误提示、失败后仍保持登录，并通过正常退出登录入口重试，保留语言切换和会话恢复验证。
- R2 / P2：发布流程缺少与待发布源码绑定的 macOS 首次使用验收确认。Q13 明确它是发布前门槛，维护者指南目前从令牌配置直接进入发布。须明确发布前文档、许可、版本说明和 macOS 验收要求，并在工作流验证已验收的源码标识。

两项已交回同一 Luna Max 代理修复，待复核。

## 第 2 轮

### Standards

0 findings。改动维持既有组件样式和单一页面主标题，路由生成文件与构建输出一致，无新增业务能力。

### Spec

0 findings。R1 已通过真实 macOS 浏览器复核：检查 Sonner 错误提示、失败后保留登录，并从正常账号菜单重试成功；等待菜单关闭后执行 axe，仅识别该场景主动注入的退出 503。

R2 已解决：工作流要求完整已验收提交 SHA 与当前运行提交相同，且两项验收确认均为真；中英文发布说明写明首次启动和文档许可门槛。五项允许／拒绝路径校验通过。

最终 Centaurus 自动化检查通过，详见 verification.md。最终 tarball 的所有模板文件与工作区逐字节一致，与 macOS 验收产物相比仅 E2E 测试文件不同，运行时代码一致。

结论：两轮直接审查完成，Standards 0 / Spec 0，交用户验收。此结论是实现审查通过，不表示已经公开发布；外部发布状态见下文。

## 实施阶段发现并已处理的事项

- 实际打包测试先因缺少模板许可证和中文说明失败，补齐交付文件后通过。
- CLI 漏列邮件加密密钥，且将环境变量更新错误地描述为仅需重启；已补齐五项秘密配置并改为重新创建 API 容器。
- auth 浏览器用例仍引用已移除的欢迎副标题；已改为验证当前账号入口。
- 实际浏览器 axe 检测到首页提醒 h5 跳级；已调整提醒标题语义并保留可访问性检查。
- GitHub runner 的 pnpm 缓存初始化顺序已改为先安装 pnpm。

## 外部发布状态

本轮等待用户验收，不创建提交、不推送、不触发 npm 发布。GitHub npm 环境已创建，私密漏洞报告已开启；NPM_TOKEN 配置及真实公开发布与发布后命令验证仍待完成，不将它们计为已通过。
